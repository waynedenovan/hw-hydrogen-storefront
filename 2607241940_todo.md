## --

"Sub Collections" that have been assigned by means of the Collection App should be displayed on the "Collection" page

## --

● Fixed and verified live. Summary:

  Two compounding root causes:
  1. Data: the store reset wiped the collection_role=main metafield off the 17 Fixed Collections — repaired by running the existing setup-fixed-collections.mjs script live against Shopify (recreated/adopted all 17 mains, reseeded 97 sub-collection→parent assignments, synced 21k product memberships).
  2. Code bug (new, not caused by the reset): the homepage/collections queries capped at collections(first: 100), but the reimport pushed total collections to 137 — the Fixed Collections sorted past that window, so they'd have stayed invisible even with the metafield fixed. Bumped to first: 250 (Shopify's max) in all three affected query sites: ($locale)._index.tsx, ($locale).collections._index.tsx, and ($locale).collections.$handle.tsx (the last one wasn't yet symptomatic but had the identical latent bug).

  Verified live on hoseworld.store via Playwright: homepage shows the Fixed Collections tiles again, and /collections/plumbing correctly shows its assigned Sub Collections. Nothing committed — you commit manually per your standing instruction.

✻ Worked for 4m 50s

## --

● Confirmed live: v2026.07.181 is rendering in the hydrogen storefront footer. Both repos versioned and rebuilt:

  - hw-hydrogen-storefront-node-docker: 2026.07.180 → 2026.07.181 (my 3 route file changes are commit #181)
  - hw-storefront-ui-node-docker: 2026.07.246 → 2026.07.253 (VERSION had drifted 6 commits behind HEAD from prior sessions that skipped the bump — corrected to match)

  Both containers rebuilt and recreated, verified live. Ready for you to commit both.

✻ Cooked for 2m 19s