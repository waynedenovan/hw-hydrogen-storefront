2607081815 claude
VERSION: 2026.07.5

"For this session, prioritize using primarily the "shopify-dev-mcp" MCP server to confirm correct pattern coding for this project especially when in "Plan" mode or "Debug" mode. Whenever debugin a bug or error first query the project_errors.db and then the "shopify-dev-mcp" MCP server. Once the task is completed update the "MCP-Bridge" MCP server and the project_errors.db. Always check the server for existing patterns before suggesting a fix. The "MCP-Bridge" MCP server has an example for context as needed if requiring search parameters."

context:
- use "plan mode" for this task where you are asked to make changes, add functionality or identify how an aspect of this porjects works.
- use "debug mode" when asked to identify any causes of the project that are not working or if you detect a bug.
- use both modes when solving issues ie, finding a bug and then planing a code fix etc.
- when indexing you are to strickly only use MCP: MCP-Bridge and db: 'project_errors.db'
- when you are testing ui, implementations and error/issue identification and correction you need to physically test them yourself using Playwright MCP Server.
- INCLUDE ONLY: /hw-storefront-ui and ../hw-hydrogen-storefront when planing and implementing any needed actions or changes
- git: I will always manually commit. Never offer or automatically try or exec a commit
- versioning: each task bumps the repo-root `VERSION` file by 1, format `yyyy.mm.{n}` (e.g. `2026.07.3`) — `{n}` is a single running counter across all tasks, it never resets month to month (only the `yyyy.mm` prefix reflects the current date). One bump per task/session, not per individual fix within a session. Record the version at the top of that task's timestamped todo file. This assistant never commits, so the `VERSION` file bump rides along in my next manual commit rather than being tagged in git directly.

## Task requirements:
- Follow each step
- Please don't guess that you need to do. Confirm and double chek you sugested coding with the "Skills" and needed coding direction found within the MCP server.

## Step 1: Planing
- ../hw-storefront-ui-node-docker:
    - The following images show the import app's selected collection, brand, sub-collection and sub_cat-collection's
        - app/errors/260708173830-errors.png
        - app/errors/260708173845-errors.png
        - app/errors/260708173901-errors.png
        - app/errors/260708173917-errors.png
        - app/errors/260708173934-errors.png
        - app/errors/260708173948-errors.png
- Main Page:
    - The following image shows the collections displayed (which are correct)
        - app/errors/260708174432-errors.png
- Collection:
    - Agricultural:
        - app/errors/260708174216-errors.png : Filter closed
        - app/errors/260708174229-errors.png : Filter open
    - Irrigation-pumps:
        - app/errors/260708174347-errors.png : Filter closed
        - app/errors/260708180647-errors.png : Filter open
    - Plumbing:
        - app/errors/260708180916-errors.png : Filter closed
        - app/errors/260708174324-errors.png : Filter open
    - As can be seen:
        - Brands are correctly shown but,
            - Sub-Collection and Sub_Cat-Collections are not pulling through correctly

- Main Page:
    - Footer is not completly at the bottom of the page
        - app/errors/260708174432-errors.png
        - app/errors/260708174508-errors.png
        - add: without changing the information already displayed
            - in the centre between the copy right anf the logo
                - version no

## Step 2: Documentation and exit
- update @2607081815_todo.md but exclude todo.md from any updating
- update with latest project plan update and any new issues found, the errors causing the issue and the step by step resolution:
    - update "MCP-Bridge" project plan
    - update the @project_errors.db

## --

---

## RESOLUTION (2026-07-08, VERSION 2026.07.5)

### Issue 1 — Sub-Collection / Sub-Cat-Collection showing "Other"

**Debug-mode investigation (query-first per session instructions):**
- `search_errors` had no direct entry; `qdrant-find` surfaced the prior root-cause entry `namespace-app-vs-custom-storefront-metafields` (2026-07-07) — the `$app`→`custom` metafield namespace fix in `hw-storefront-ui-node-docker/app/services/product.server.js`.
- Storefront reader code was ruled out first: `app/routes/($locale).collections.$handle.tsx` already queries `custom.sub_collection` / `custom.sub_cat_collection` and renders real values when present (`'Other'` at lines 50/54 is only the null fallback) — proven live by irrigation-pumps showing JA PUMPS / JB ELECTRIC MOTORS while agricultural/plumbing showed only Other.
- Live Storefront API census: 366 products, only **86** had the metafields — exactly the set from the 2026-07-07 host-side backfill. Today's "Apply Changes" (47 created + 118 updated, per the task's import screenshots) had added **zero**.
- Local Prisma DB and the supplier JSON were both complete (all 165 imported products carry `assignedMajorDescription`/`assignedSubCatDescription`), so the data was fine — the write to Shopify was the failure.
- **Root cause (smoking gun):** the running `storefront-ui` Docker container was created 2026-07-07 09:15 UTC — *before* that day's namespace fix landed. A fixed image was built 2026-07-08 14:57 but the container was **never recreated**, so today's import still ran the old code and wrote metafields under the app-reserved `$app` namespace (invisible to the Hydrogen storefront's client).
- Confirmed the correct pattern against shopify-dev-mcp (metafieldsSet + PUBLIC_READ metafield definitions; GraphQL validated against the 2026-04 Admin schema).

**Step-by-step fix:**
1. Rescued the runtime-fetched supplier files (`suppliers/agrinet/{prod,price,stock}/2607071043xx-*.json`) out of the old container via `docker cp` — they existed only in the container FS and would have been lost on recreate. Copied them back into the new container after recreation too (the runtime image's Dockerfile never includes `suppliers/`).
2. `docker compose build && docker compose up -d` in `hw-storefront-ui-node-docker` — container now runs the `custom`-namespace code (verified `METAFIELD_NAMESPACE = "custom"` + `PUBLIC_READ` in the new container's server bundle).
3. Ran the existing host-side backfill (`set -a; source .env; set +a; npx tsx scripts/backfill-custom-namespace.mjs`) — **165/165 products re-synced, 0 failed**. (Note: `.env` must be sourced manually for host-side scripts; also the script's "definition FAILED" lines are cosmetic — Shopify now returns code `TAKEN` with message "Key is in use…", which the script's `/already exists|taken/i` message-text regex doesn't match.)
4. Census after backfill: 157/157 storefront-visible products had both metafields (the other 8 tracked products are DRAFT, invisible to the Storefront API by design).
5. **Orphan cleanup (user-approved decision):** 282 Shopify products had no local Prisma row (pre-DB-wipe leftovers: 153 PASCALI, 76 MOTORELLI, plus demo brands BrassCraft/PolyFlow/CamPro/SnapLock/HoseWorld/ClampMaster/SprayPro/AIRSHRINK/CINCO/TORRENTI/SPRING VALLEY/HILLMAN/MECH) — unreachable by every import-app code path (update/backfill/stale-delete all key off Prisma rows). New script `hw-storefront-ui-node-docker/scripts/delete-orphan-products.mjs` (dry-run by default → `scripts/orphan-products-report.md`; `--delete` to execute). Dry-run reviewed (no tracked vendors present), then deleted **282/282, 0 failed**.
6. Final census: **157 products on the storefront, 157 with sub_collection + sub_cat_collection — 100% coverage.**

**Verified live (Playwright, hoseworld.store):**
- Agricultural: TRUNEL → FZ AGRICULTURAL → FZAE ACCESSORIES (was TRUNEL → Other → Other); filter panel shows the real names.
- Plumbing: DUTTON PLASTICS → HD FASTENERS / HI PLUMBING ACC / HL VALVES → 7 real sub-cats.
- Irrigation-pumps: PASCALI/WORTEX/WEG → JA PUMPS / JB ELECTRIC MOTORS → 12 real sub-cats; zero "Other" anywhere on all three pages.
- Console: only pre-existing errors (CSP font/cloudflare-beacon blocks, PerfKit storefrontId, known missing supplier-image 404s tracked in missing_images.md).

### Issue 2 — footer bottom + version number

- Live measurement showed the deployed site was already flush (`footer.bottom === scrollHeight`, gap 0) at 1280×720, 1920×1000 and the exact 1915×1006 screenshot viewport — the prior session's `100dvh` fix is active; the screenshots' white strip + "$" has no corresponding DOM node (most plausibly a stale/cached tab or browser-chrome artifact).
- Defensive hardening anyway: `app/styles/reset.css` — new `html:has(> body.home-page/…)` rule gives `html` an explicit black background on the five background-image pages, stopping body-background canvas propagation so any real-mobile overscroll/toolbar strip below the footer renders black (invisible) instead of the page image/white. Classless pages (collections, policies) keep their white canvas — no visual change.
- Version number: `app/components/Footer.tsx` imports the repo-root `VERSION` file via Vite `?raw` and renders `v{VERSION}` as a new centre child of the existing `FooterBrandBar` row — copyright (left) and logo/Powered-by (right) untouched. `app/styles/app.css`: `.footer-version` + a `min-width: 45em` rule (`.footer-brand > * { flex: 1 }`) so the version sits at the row's true centre on desktop (measured offset 0px) while mobile keeps the wrap behaviour.
- `VERSION` bumped 2026.07.4 → **2026.07.5** (displayed in the footer).
- Rebuilt/recreated the hydrogen container (no source bind-mount). Verified at 1920×1000 and 360×640: gap 0, no horizontal overflow, brand row wraps cleanly on mobile; full-page screenshot confirms the footer ends the page with the version centred.

### Files changed
- `hw-hydrogen-storefront-node-docker`: `app/components/Footer.tsx`, `app/styles/app.css`, `app/styles/reset.css`, `VERSION`, this file.
- `hw-storefront-ui-node-docker`: `scripts/delete-orphan-products.mjs` (new), `scripts/orphan-products-report.md` (generated), `suppliers/agrinet/{prod,price,stock}/2607071043xx-*.json` (rescued from container); container rebuilt/recreated (no source changes).

### New issues found (not in scope, flagged)
- Transient `ETIMEDOUT`/`fetch failed` from the hydrogen container to Shopify right after startup (also the cause of the harmless "publications" errors in today's import screenshots) — resolves on retry; worth a retry/backoff wrapper someday.
- The footer still renders policy links twice (FooterMenu + hardcoded FooterUtilities) — pre-existing, flagged in the 2607081600 session, still open.

### MCP-Bridge / project_errors.db update
No write tool for `project_errors.db` exists in this session's toolset (`search_errors` is read-only); per established convention the session summary + reusable patterns were stored via `qdrant-store` (MCP-Bridge): stale-container-runs-pre-fix-code, runtime-fetched-files-live-only-in-container-fs, orphan-shopify-products-unreachable-by-import-pipeline.

## --

● Task complete — both issues fixed, verified live on hoseworld.store, nothing committed (yours to do as usual).

  Issue 1: Sub-Collection / Sub-Cat showing "Other" — fixed, 100% coverage

  Root cause was not the storefront code — it was a stale Docker container. The import app's running container was created the morning of Jul 7, before that
  day's $app→custom metafield-namespace fix landed. A fixed image was even built today at 14:57, but the container was never recreated — so today's "Apply
  Changes" import wrote all metafields under the invisible $app namespace again. Evidence: only 86 of 366 products had the metafields (exactly yesterday's
  backfill set), while the local DB and supplier JSON were complete for all 165 imported products.

  The fix, in hw-storefront-ui-node-docker:
  1. Rescued the runtime-fetched supplier files (Jul-7 products/prices/stock JSONs) out of the old container — they existed only in its filesystem and would
  have been destroyed.
  2. Rebuilt and recreated the container (now verified running the custom-namespace code).
  3. Ran the existing backfill script: 165/165 products re-synced, 0 failures.
  4. Per your "delete orphans" decision: new scripts/delete-orphan-products.mjs (dry-run report first, reviewed, then --delete) removed 282 orphan products
  with no local DB record — old PASCALI/MOTORELLI leftovers and the demo brands. Final state: 157 storefront-visible products, all 157 with real
  Sub-Collection and Sub-Cat values — zero "Other" anywhere.

  with no local DB record — old PASCALI/MOTORELLI leftovers and the demo brands. Final state: 157 storefront-visible products, all 157 with real
  Sub-Collection and Sub-Cat values — zero "Other" anywhere.

  Verified live: Agricultural now shows TRUNEL → FZ AGRICULTURAL → FZAE ACCESSORIES; Plumbing shows HD/HI/HL with all 7 sub-cats; Irrigation-pumps shows
  JA/JB with all 12 sub-cats — in both the grouping tree and the filter panel.

  Issue 2: Footer — version added, bottom hardened

  - Live measurement showed the footer already flush at every viewport, including the exact size of your screenshot — the earlier dvh fix is deployed, and
  the white strip/"$" has no DOM node (likely a stale tab; try a hard refresh). I still hardened it: the background-image pages now paint html black, so any
  real-mobile overscroll strip renders black instead of white/image.
  - v2026.07.5 now sits at the true centre of the footer brand row between the copyright and the Rogue Nation logo (reads the repo-root VERSION file at build
  time), wrapping cleanly down to 360px. Existing content untouched.

  Documentation

  2607081815_todo.md has the full investigation and resolution, VERSION bumped to 2026.07.5, session summary + three reusable patterns stored in MCP-Bridge.
  One flag for a future task: the hydrogen container's first request after a restart 500s with a transient Shopify ETIMEDOUT (retry succeeds) — a
  retry/backoff wrapper would smooth that over.

✻ Churned for 31m 38s