2607281510 claude

"For this session, prioritize using primarily the "shopify-dev-mcp" MCP server to confirm correct pattern coding for this project especially when in "Plan" mode or "Debug" mode. Whenever debuging a bug or error first contact the MCP server: hoseworld-dev-knowledge and then the "shopify-dev-mcp" MCP server. Once the task is completed update the "MCP-Bridge" MCP server. Always check the server for existing patterns before suggesting a fix. The "MCP-Bridge" MCP server has an example for context as needed if requiring search parameters."

context:
- use "plan mode" for this task where you are asked to make changes, add functionality or identify how an aspect of this porjects works.
- use "debug mode" when asked to identify any causes of the project that are not working or if you detect a bug.
- use both modes when solving issues ie, finding a bug and then planing a code fix etc.
- when indexing you are to strickly only use MCP: MCP-Bridge i.e. hoseworld-dev-knowledge
- when you are testing ui, implementations and error/issue identification and correction you need to physically test them yourself using Playwright MCP Server.
- INCLUDE ONLY: GIT branch folders when planing and implementing any needed actions or changes
- git: I will always manually commit. Never offer or automatically try or exec a commit

## Task requirements:
- Follow each step
- Please don't guess that you need to do. Confirm and double chek you sugested coding with the "Skills" and needed coding direction found within the MCP server.

## Step 1: Planing
- Tier 1:
    - I need to add at the bottom in the footer alongside "Privacy Policy, Terms of Service, Refund Policy, Shipping Policy"
        - Image Credit
            - Must open a text frame above everything else the says
                - Images on Pixbay by:
                    We thank the photogrephers for their work and allowing us to use their images
                    Bru-nO
                    johnnaturephotos
                    trapezemike
                    anncapictures
                    Pexels
                    ThMilherou
                    jp26jp
                    jarmoluk
                    cocoparisienne
                    VariousPhotography
                    Life-Of-Pix
                    chulmin1700
                    malateronald
                    igorovsyannykov
    - hw-storefront-ui-node-docker:
        - Please provide a option to be able to update the above message when needed
        - Please provide a option to be able to update the links on the hw-hydrogen-storefront-node-docker
            - Privacy Policy
            - Terms of Service
            - Refund Policy
            - Shipping Policy

## Step 2: Documentation and exit
- Update Verion to reflect the Commit count as per every task
- update @2607281510_todo.md but exclude todo.md from any updating
- update with latest updated project plan and any new issues found, the errors causing the issue and the step by step resolution:
    - hoseworld-dev-knowledge and any other MCP server that is relevent

## --

## STATUS: COMPLETE, VERIFIED LIVE

Mid-session addition (not in the original written task above, given verbally): also remove the Tier 1 homepage's background hero image and make it solid black, matching Tier 2 collection pages.

### Planning (plan mode)
Ran 3 parallel Explore agents against both repos before writing any code:
1. Footer/policy-link structure + existing modal patterns in `hw-hydrogen-storefront-node-docker`.
2. Admin settings-page patterns + shop/collection metafield conventions in `hw-storefront-ui-node-docker`.
3. Tier 1 vs Tier 2 background CSS.

Findings: the four policy links are hardcoded `<NavLink>`s in `Footer.tsx`'s `FooterUtilities()` — unrelated to the CMS-driven `FooterMenu`. No prior Image Credit / Pixabay code existed anywhere (net new). The established, most-consistent path for "admin app writes storefront-visible content" is `collections.server.js`'s `setCollectionMetafields`/`ensureCollectionMetafieldDefinitions` pattern (custom-namespace metafields, `access.storefront: PUBLIC_READ`) — extended here to SHOP-level metafields (no prior shop-metafield writer existed). Best modal pattern to clone: `GalleryCarousel` in `($locale).products.$handle.tsx` (Escape/backdrop-click close, `fixed inset-0 z-50 bg-black/80`, `role="dialog" aria-modal="true"`). Tier 1 vs Tier 2 background difference was a single CSS rule (`.page-bg.home-page` vs `.page-bg.collection-page` in `reset.css`).

Plan approved by Wayne before implementation began.

### Fix Applied

**`hw-hydrogen-storefront-node-docker`** (this repo):
- `app/styles/reset.css`: `.page-bg.home-page` background-image swapped for `background: var(--color-dark);`, mirroring the existing `.page-bg.collection-page` rule. No JSX changes — `PageLayout.tsx` still renders the `home-page` class exactly as before, so the `main` padding rule and the `body.home-page{background:#000}` canvas fallback are unaffected. Note: the site-wide `.header-hero` pump-image banner (Header.tsx, app.css:480) is unrelated infrastructure shared by every page including Tier 2 — confirmed via live Tier 2 screenshot before touching anything, so it was correctly left alone.
- `app/root.tsx`: new `STOREFRONT_SETTINGS_QUERY` (`shop { metafield(namespace:"custom", key:"...") }` x5, aliased) + `storefrontSettings` deferred promise added to the loader return, same `any`-cast/`.catch(() => null)` pattern as the existing `footerBanner` query.
- `app/components/PageLayout.tsx`: `storefrontSettings` prop threaded through to `<Footer>`.
- `app/components/Footer.tsx`: `FooterUtilities` now resolves `storefrontSettings` via nested `<Suspense><Await>` (same pattern as `footerBanner`), renders the 4 policy `NavLink`s with `settings?.xUrl?.value || '/policies/...'` fallback, and adds an "Image Credit" `<button>` that opens a new `ImageCreditModal` (cloned from `GalleryCarousel`'s structure) showing `settings?.imageCreditText?.value || DEFAULT_IMAGE_CREDIT_TEXT` — the literal Pixabay credit text from the task, hardcoded as a fallback so it works even before the admin page is ever used.

**`hw-storefront-ui-node-docker`**:
- `prisma/schema.prisma`: new `StorefrontSettings` singleton model (same shape/spirit as the existing `ShippingDefaults` singleton). Migration `20260728133151_add_storefront_settings` generated and applied live against the bind-mounted `dev.sqlite`.
- `app/services/storefrontSettings.server.js` (new): `ensureShopMetafieldDefinitions`/`setShopMetafields`, mirroring `collections.server.js`'s collection-metafield helpers exactly (including the metafieldsSet-rejects-blank-values → metafieldsDelete gotcha) but targeting the shop GID instead of a collection.
- `app/routes/app.storefront-settings.jsx` (new): plain Prisma CRUD loader/action (no `jobs.server.js` — single-row read/write, not catalog-scale), copying `app.shipping.settings.jsx`'s shape. Textarea for the credit text + 4 URL inputs; on save, writes the Prisma row AND pushes the same 5 values out as shop metafields.
- `app/routes/app.jsx`: added `<s-link href="/app/storefront-settings">Storefront Settings</s-link>` nav entry.

### Version / Rebuild
- VERSION bumped 2026.07.187 → 2026.07.188 (this repo's per-task counter convention).
- `hw-hydrogen-storefront-node-docker`: `docker compose build && docker compose up -d` (twice — once for the code changes, once more after the VERSION bump so the footer picks it up; no source bind-mount on this container).
- `hw-storefront-ui-node-docker`: `docker compose build && docker compose up -d` (its `docker-start` runs `prisma migrate deploy` automatically; migration was also already applied directly on the host beforehand).

### Verification
- `npm run typecheck` / `react-router build` (hydrogen repo): no new errors introduced (pre-existing `storefrontapi.generated` module-not-found errors are a pre-existing, repo-wide condition — codegen hasn't been run against a live Storefront API in this environment — confirmed identical baseline via `git stash`).
- `npx eslint` + `npx react-router build` (ui repo): clean, new route bundles correctly.
- **Admin round-trip** (Playwright couldn't reach the embedded admin route directly — this sandboxed browser has no Shopify admin login session, confirmed by hitting the pre-existing, known-good `/app/shipping/settings` route the same way and getting the identical 410/"Unhandled Thrown Response" bounce, so this is standard unauthenticated-embedded-app behavior, not a bug). Exercised the loader/action logic directly instead, via the project's own documented `client_credentials` `buildAdmin()` script pattern (see `scripts/setup-fixed-collections.mjs`): seeded the singleton row, updated it, called `ensureShopMetafieldDefinitions` (created all 5 SHOP-owner definitions with `storefront: PUBLIC_READ`), called `setShopMetafields`, and read the values back via both the Admin API and a direct Storefront API curl — all matched. Cleanup reverted the test value and exercised the delete-on-blank path too.
- **Live storefront (Playwright, https://hoseworld.store)**: homepage background confirmed solid black, no hero image, matching `/collections/power-products` (Tier 2) side by side — the shared `.header-hero` pump banner correctly present on both (unaffected, as intended). Footer shows "Image Credit" next to the 4 policy links; clicking it opens the modal above everything else with the correct fallback text (all 14 photographer credits + thank-you line); Escape key closes it. Set a distinguishable marker string via the shop metafield, restarted the container to clear Hydrogen's `CacheLong()` in-memory sub-request cache, and confirmed the marker appeared live in the modal — proving the full write→read pipeline (admin service → shop metafield → Storefront API → root loader → Footer) actually works, not just that each piece compiles. Reverted to the real default text and confirmed it displays correctly afterward. Footer confirms `v2026.07.188` live.
- Console errors present (CSP-blocked Google Fonts/Cloudflare beacon, PerfKit storefrontId) are pre-existing and unrelated, same as prior sessions' notes.

### New issues found, not fixed (out of this task's scope)
- `storefront.CacheLong()` on the new `STOREFRONT_SETTINGS_QUERY` (same as the pre-existing `footerBanner` query) means an admin edit to the credit text or policy URLs may take up to Hydrogen's CacheLong TTL (~1 day) to appear on the live storefront without a container restart — this is existing, accepted site behavior (identical to how `footerBanner` already worked), not a regression, but worth knowing if Wayne edits settings and doesn't see the change immediately.
- The homepage's own `<h2>Online</h2>` heading still shows its sitewide dark contrast-pill background (reset.css `h1..h5` rule) since the homepage isn't `.page-card`-wrapped (intentionally — that's what gives its `CollectionCard` tiles their established chip look). This is a pre-existing, separate visual detail unrelated to the background-image removal; flagged in the plan, not changed.

Not committed — Wayne commits manually per standing instruction.

## --

● Summary

  Delivered all 3 asks: (1) a new "Image Credit" link in the storefront footer that opens a text-frame modal crediting the 14 Pixabay photographers, (2) an admin settings page in hw-storefront-ui-node-docker (`/app/storefront-settings`) letting Wayne edit that credit text and the 4 policy link URLs, surfaced to the storefront via shop-level metafields (same pattern as the existing collection-metafield mechanism), and (3) the Tier 1 homepage background hero image removed and replaced with solid black, matching Tier 2 collection pages exactly (the shared pump-image header banner was correctly left untouched, since it already appears identically on both tiers).

  Full read/write round trip verified live: admin service → Prisma singleton row → shop metafield write → Storefront API → Hydrogen root loader → footer modal, using a distinguishable test marker that appeared correctly then was reverted. Embedded admin UI itself couldn't be click-tested via Playwright (no Shopify admin login session in this sandbox — confirmed this is standard behavior, not a bug, by getting the identical bounce from an existing known-good admin route). VERSION bumped to 2026.07.188, both containers rebuilt/recreated, footer confirms it's live. Documented here and logged to MCP-Bridge (hoseworld-dev-knowledge). Not committed — that's yours to do manually, across both repos.

  ## --

  ● All three deliverables are complete and verified live:

  1. Footer "Image Credit" modal — new link opens a text-frame overlay above everything else, crediting all 14 Pixabay photographers.
  2. Admin-editable content — new /app/storefront-settings page in hw-storefront-ui-node-docker lets Wayne edit the credit text and all 4 policy link URLs, backed by shop metafields.
  3. Tier 1 background fix — homepage hero image removed, replaced with solid black matching Tier 2 pages (confirmed the shared .header-hero pump banner was correctly left untouched, since it's already identical on both tiers).

  Full write→read pipeline (admin service → Prisma → shop metafield → Storefront API → Hydrogen footer) was verified live with a test marker, then reverted cleanly. VERSION bumped to 2026.07.188, both containers rebuilt and confirmed live, documentation written to both repos' 2607281510_todo.md, and the session logged to MCP-Bridge. Nothing was committed — that's yours to do manually across both repos.

✻ Sautéed for 32m 49s